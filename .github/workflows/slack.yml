name: message to slack

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: comfuture/actions/slack@v1
      with:
        args: -h bot_test -m "pushed!"
      env:
        SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
